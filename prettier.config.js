// Although we mostly use Prettier via eslint-plugin-prettier, it's useful to
// be able to run Prettier by itself. This enables things like inline
// snapshots to format generated code correctly.
module.exports = {
  singleQuote: true,
  trailingComma: 'all',
};
